---
/**
 * Shared bento-style grid for landing page card sections (Features,
 * Who uses it, ...). Layout classes (.jg-bento, .jg-bento-w2, .jg-bento-h2)
 * live in src/styles/custom.css since they are shared across components.
 * `not-content` opts the cards out of Starlight's markdown content styling.
 */
---

<div class="jg-bento not-content"><slot /></div>
